# The project is its own pnpm workspace root, so pnpm never walks up into a
# parent workspace (e.g. $HOME or a sibling monorepo) when installing.
packages:
  - .

allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
